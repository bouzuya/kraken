import beater from 'beater';
import * as assert from 'power-assert';

import { listFiles } from '../../src/utils/fs';

const { test } = beater();

test('fs.listFiles', () => {
  // TODO
  assert(listFiles);
});
