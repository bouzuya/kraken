import { listFiles } from '../../src/utils/fs';
import { Test, assert, test } from '../helper';

const tests1: Test[] = [
  test('fs.listFiles', () => {
    // TODO
    assert(listFiles);
  })
];

export { tests1 as tests };
